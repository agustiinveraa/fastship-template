import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Next.js automatically detects src/ directory, no additional config needed
};

export default nextConfig;
